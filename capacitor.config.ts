import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.crud.Onkar',
  appName: 'Cloud Wallet',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
